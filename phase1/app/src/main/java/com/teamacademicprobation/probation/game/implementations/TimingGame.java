package com.teamacademicprobation.probation.game.implementations;

import com.teamacademicprobation.probation.game.Game;
/**
 * A game where there is a bar with a target box and a moving line, and the player attempts to time
 * their taps so the line ends up inside the target box.
 */
public class TimingGame extends Game {
  @Override
  public void play() {}

  @Override
  public void render() {}
}
