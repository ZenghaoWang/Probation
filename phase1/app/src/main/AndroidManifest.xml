<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teamacademicprobation.probation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".game.implementations.timinggame.TimingGameActivity"
            android:exported="true"></activity>
        <activity android:name=".game.implementations.triviagame.TriviaScoreScreenActivity" />
        <activity
            android:name=".game.implementations.triviagame.TriviaGameActivity"
            android:exported="true" />
        <activity
            android:name=".ui.LoginActivity"
            android:label="Log In">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".game.implementations.tappinggame.TapGameActivity" />
    </application>

</manifest>